import React from "react";

const StripCanvas: React.FC = () => {
  return (
    <div style={{ width: "1200px", height: "100%", backgroundColor: "#f0f0f0" }}>
      {/* This will eventually contain the punched holes */}
    </div>
  );
};

export default StripCanvas;
